- const navList = [
-   {name: 'Page not found', url: '/404'},
- ];

aside.aside
  nav(aria-label='navigation').navigation
    ul.list
      each item in navList
        li.list__item
          a(href=item.url data-link).list__link=item.name
